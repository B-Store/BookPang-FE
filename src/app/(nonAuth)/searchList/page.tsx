import SearchedData from "@/components/SearchedData";
import React from "react";

const searchList = () => {
  return (
    <div>
      <SearchedData />
    </div>
  );
};

export default searchList;
